# 🏗️ Technical Design Document — Edify-AI

> **Version:** 1.0  
> **Last Updated:** January 25, 2026  
> **Status:** Active Development

---

## 📐 Architecture Overview

### High-Level System Design

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                   CLIENT LAYER                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐               │
│  │   Next.js App    │  │ Browser Extension│  │   Mobile (PWA)   │               │
│  │  (React + tRPC)  │  │   (Chrome/Edge)  │  │    (Future)      │               │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘               │
└───────────┼─────────────────────┼─────────────────────┼─────────────────────────┘
            │                     │                     │
            ▼                     ▼                     ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                   API GATEWAY                                    │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐               │
│  │   tRPC Router    │  │  REST API (Hono) │  │   MCP Server     │               │
│  │   (Type-safe)    │  │  (Multi-Agent)   │  │  (AI Assistant)  │               │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘               │
└───────────┼─────────────────────┼─────────────────────┼─────────────────────────┘
            │                     │                     │
            ▼                     ▼                     ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              APPLICATION LAYER                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌────────────┐ │
│  │   Auth      │ │   Resume    │ │   Course    │ │  Interview  │ │  Career    │ │
│  │   Module    │ │   Module    │ │   Module    │ │   Module    │ │  Advisor   │ │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘ └────────────┘ │
└─────────────────────────────────────────────────────────────────────────────────┘
            │                     │                     │
            ▼                     ▼                     ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                               AI / AGENT LAYER                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────────────────┐      │
│  │                    LangGraph Multi-Agent Orchestrator                  │      │
│  ├───────────────────────────────────────────────────────────────────────┤      │
│  │ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────┐ │      │
│  │ │ Skills  │ │  Exp    │ │  Edu    │ │Projects │ │  Meta   │ │Roadmap│ │      │
│  │ │ Agent   │ │ Agent   │ │ Agent   │ │ Agent   │ │ Agent   │ │ Agent │ │      │
│  │ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └───────┘ │      │
│  └───────────────────────────────────────────────────────────────────────┘      │
│                                      │                                           │
│                    ┌─────────────────┴─────────────────┐                        │
│                    │          AI Gateway               │                        │
│                    │  (Model Router + Rate Limiter)    │                        │
│                    ├───────────────────────────────────┤                        │
│                    │ Vertex AI │ OpenAI │ Gemini │Local│                        │
│                    └───────────────────────────────────┘                        │
└─────────────────────────────────────────────────────────────────────────────────┘
            │                     │                     │
            ▼                     ▼                     ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              DATA & STORAGE LAYER                                │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  PostgreSQL  │  │    Redis     │  │  Vector DB   │  │Object Storage│         │
│  │   (Neon)     │  │  (Upstash)   │  │ (Supabase)   │  │    (GCS)     │         │
│  │              │  │              │  │              │  │              │         │
│  │ • Users      │  │ • Sessions   │  │ • Embeddings │  │ • Resumes    │         │
│  │ • Resumes    │  │ • Cache      │  │ • Similarity │  │ • Documents  │         │
│  │ • Courses    │  │ • Rate Limit │  │ • Search     │  │ • Media      │         │
│  │ • Progress   │  │              │  │              │  │              │         │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────────────────────┘
            │                     │
            ▼                     ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           BACKGROUND PROCESSING                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────────────────┐       │
│  │                         Inngest Event Bus                            │       │
│  ├──────────────────────────────────────────────────────────────────────┤       │
│  │ ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐          │       │
│  │ │  Resume    │ │  Course    │ │ Interview  │ │   Job      │          │       │
│  │ │  Worker    │ │  Worker    │ │  Worker    │ │  Scraper   │          │       │
│  │ └────────────┘ └────────────┘ └────────────┘ └────────────┘          │       │
│  └──────────────────────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🧩 Component Design

### 1. Frontend Architecture (Next.js 14+)

#### Directory Structure
```
app/
├── (home)/                    # Protected home routes
│   ├── ats/                   # ATS analysis feature
│   └── dashboard/             # Main dashboard
├── (course)/                  # Course-related routes
│   ├── course/                # Individual course view
│   ├── course-dashboard/      # Learning dashboard
│   ├── create-course/         # AI course generator
│   └── path/                  # Career roadmap
│       └── personalized/      # Personalized pathways
├── (mock)/                    # Mock interview routes
│   └── mock/                  # Interview sessions
├── (meeting)/                 # Video meeting routes
├── (agent-dashboard)/         # AI agent interface
├── career-advisor/            # Career advisory chat
├── api/                       # API routes
│   ├── trpc/                  # tRPC handlers
│   ├── inngest/               # Inngest webhooks
│   └── [feature]/             # Feature-specific APIs
└── call/                      # Video call integration
```

#### State Management
```typescript
// Pattern: React Query + Context for global state
┌─────────────────────────────────────────────────┐
│             QueryClientProvider                  │
│  ┌───────────────────────────────────────────┐  │
│  │            ThemeProvider                   │  │
│  │  ┌─────────────────────────────────────┐  │  │
│  │  │        ResumeInfoProvider            │  │  │
│  │  │  ┌───────────────────────────────┐  │  │  │
│  │  │  │       Feature Components       │  │  │  │
│  │  │  └───────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

#### Key Technologies
| Technology | Purpose |
|------------|---------|
| Next.js 14+ | App Router, Server Components, API Routes |
| tRPC | End-to-end type-safe API |
| TanStack Query | Server state management |
| Tailwind CSS | Utility-first styling |
| NyxbUI | Component library |
| Zustand | Client state (where needed) |

---

### 2. Backend Architecture

#### Python Multi-Agent System (LangGraph)
```
backend/src/
├── agents/                    # Specialized AI agents
│   ├── resume_extractor.py    # Extract structured resume data
│   ├── skills_agent.py        # Skills matching & scoring
│   ├── experience_agent.py    # Work experience analysis
│   ├── education_agent.py     # Education evaluation
│   ├── projects_agent.py      # Project relevance scoring
│   └── meta_agent.py          # Seniority & domain matching
├── graph/
│   └── orchestrator.py        # LangGraph workflow definition
├── models/
│   └── schemas.py             # Pydantic data models
├── deploy/
│   └── agent_engine_deploy.py # Vertex AI deployment
└── config.py                  # Configuration management
```

#### Agent Orchestration Flow
```
                              ┌─────────────────────┐
                              │   Resume + JD Input  │
                              └──────────┬──────────┘
                                         │
                              ┌──────────▼──────────┐
                              │   Resume Extractor   │
                              │       Agent          │
                              └──────────┬──────────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    │                    │                    │
           ┌────────▼────────┐ ┌────────▼────────┐ ┌────────▼────────┐
           │  Skills Agent   │ │Experience Agent │ │Education Agent  │
           │                 │ │                 │ │                 │
           │ Weight: 0.30    │ │ Weight: 0.30    │ │ Weight: 0.15    │
           └────────┬────────┘ └────────┬────────┘ └────────┬────────┘
                    │                    │                    │
           ┌────────▼────────┐ ┌────────▼────────┐
           │ Projects Agent  │ │   Meta Agent    │
           │                 │ │                 │
           │ Weight: 0.15    │ │ Weight: 0.10    │
           └────────┬────────┘ └────────┬────────┘
                    │                    │
                    └─────────┬──────────┘
                              │
                    ┌─────────▼─────────┐
                    │   Weighted Score   │
                    │   Aggregation      │
                    └─────────┬─────────┘
                              │
                    ┌─────────▼─────────┐
                    │  Analysis Report   │
                    │  + Recommendations │
                    └───────────────────┘
```

---

### 3. Database Design

#### Core Schema (Drizzle ORM)
```typescript
// db/schema/

// User & Authentication
users {
  id: uuid PRIMARY KEY
  kinde_id: string UNIQUE
  email: string UNIQUE
  name: string
  avatar_url: string
  occupation: string
  interests: string[]
  skills: string[]
  created_at: timestamp
  updated_at: timestamp
}

// Resume Management
resumes {
  id: uuid PRIMARY KEY
  user_id: uuid REFERENCES users
  title: string
  file_url: string
  parsed_data: jsonb
  status: enum('pending', 'parsed', 'error')
  created_at: timestamp
}

resume_analysis {
  id: uuid PRIMARY KEY
  resume_id: uuid REFERENCES resumes
  job_description: text
  overall_score: decimal
  skills_score: decimal
  experience_score: decimal
  education_score: decimal
  projects_score: decimal
  meta_score: decimal
  recommendations: jsonb
  created_at: timestamp
}

// Learning & Courses
courses {
  id: uuid PRIMARY KEY
  user_id: uuid REFERENCES users
  title: string
  description: text
  content: jsonb
  status: enum('draft', 'active', 'completed')
  created_at: timestamp
}

chapters {
  id: uuid PRIMARY KEY
  course_id: uuid REFERENCES courses
  title: string
  content: jsonb
  order: integer
  completed: boolean
}

// Mock Interviews
mock_sessions {
  id: uuid PRIMARY KEY
  user_id: uuid REFERENCES users
  job_title: string
  job_description: text
  questions: jsonb
  status: enum('pending', 'active', 'completed')
  feedback: jsonb
  scores: jsonb
  created_at: timestamp
}

// Career Tracking
tracked_jobs {
  id: uuid PRIMARY KEY
  user_id: uuid REFERENCES users
  company: string
  title: string
  url: string
  status: enum('saved', 'applied', 'interviewing', 'offer', 'rejected')
  notes: text
  created_at: timestamp
}
```

#### Entity Relationship Diagram
```
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│    users     │───┐   │   resumes    │───────│resume_analysis│
└──────────────┘   │   └──────────────┘       └──────────────┘
                   │
                   ├───│   courses    │───────│   chapters   │
                   │   └──────────────┘       └──────────────┘
                   │
                   ├───│ mock_sessions │
                   │   └──────────────┘
                   │
                   └───│ tracked_jobs  │
                       └──────────────┘
```

---

### 4. API Design

#### tRPC Router Structure
```typescript
// trpc/routers/

appRouter
├── auth
│   ├── getSession()
│   └── logout()
├── user
│   ├── getProfile()
│   ├── updateProfile()
│   └── getProgress()
├── resume
│   ├── list()
│   ├── upload()
│   ├── analyze()
│   ├── getAnalysis()
│   └── optimize()
├── course
│   ├── list()
│   ├── create()
│   ├── get()
│   ├── updateProgress()
│   └── complete()
├── roadmap
│   ├── generate()
│   ├── get()
│   └── regenerate()
├── mock
│   ├── startSession()
│   ├── submitAnswer()
│   ├── getResults()
│   └── getHistory()
└── jobs
    ├── track()
    ├── list()
    └── updateStatus()
```

#### REST API Endpoints (Python Backend)
```yaml
POST /api/analyze
  Description: Analyze resume against job description
  Request:
    resume_text: string
    job_description: string
  Response:
    overall_score: number
    section_scores: object
    recommendations: array
    skill_gaps: array

POST /api/extract
  Description: Extract structured data from resume
  Request:
    resume_text: string
  Response:
    personal_info: object
    skills: array
    experience: array
    education: array
    projects: array

POST /api/roadmap
  Description: Generate personalized career roadmap
  Request:
    user_profile: object
    target_role: string
    skill_gaps: array
    timeframe: string
  Response:
    roadmap: object
    milestones: array
    estimated_duration: string
```

---

### 5. AI/ML Architecture

#### Model Context Protocol (MCP) Server
```typescript
// mcp/server.ts

MCP Server
├── Tools
│   ├── generate_course        # Create personalized course
│   ├── analyze_resume         # Run ATS analysis
│   ├── start_interview        # Begin mock interview
│   ├── get_roadmap           # Fetch career roadmap
│   ├── search_jobs           # Search job listings
│   └── track_progress        # Update learning progress
├── Resources
│   ├── user_profile          # Current user context
│   ├── resume_data           # Parsed resume info
│   ├── course_progress       # Learning status
│   └── interview_history     # Past interviews
└── Prompts
    ├── career_advisor        # Career guidance prompt
    ├── interview_coach       # Interview prep prompt
    └── learning_mentor       # Learning guidance prompt
```

#### AI Gateway Design
```
┌─────────────────────────────────────────────────────────────┐
│                        AI Gateway                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │   Request   │  │   Model     │  │   Response  │          │
│  │   Router    │──│   Selector  │──│   Handler   │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
│         │                │                │                  │
│  ┌──────▼────────────────▼────────────────▼──────┐          │
│  │              Middleware Layer                  │          │
│  │  • Rate Limiting  • Cost Tracking  • Logging  │          │
│  └───────────────────────────────────────────────┘          │
│                          │                                   │
│  ┌───────────────────────▼───────────────────────┐          │
│  │               Model Providers                  │          │
│  │ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────┐ │          │
│  │ │Vertex AI│ │ OpenAI  │ │ Gemini  │ │ Local │ │          │
│  │ └─────────┘ └─────────┘ └─────────┘ └───────┘ │          │
│  └───────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────┘
```

---

### 6. Background Processing (Inngest)

#### Event-Driven Architecture
```typescript
// inngest/functions/

Events & Functions
├── user.onboarded
│   └── → generateInitialRoadmap()
├── resume.uploaded
│   └── → parseResume()
│       └── → createEmbeddings()
├── resume.analyze
│   └── → runMultiAgentAnalysis()
├── course.generate
│   └── → generateCourseContent()
│       └── → fetchYouTubeContent()
├── interview.completed
│   └── → analyzeInterview()
│       └── → generateFeedback()
└── job.matched
    └── → sendNotification()
```

#### Worker Flow Example
```
┌────────────────┐     ┌────────────────┐     ┌────────────────┐
│  API Request   │────▶│  Event Queue   │────▶│    Worker      │
│                │     │   (Inngest)    │     │                │
└────────────────┘     └────────────────┘     └───────┬────────┘
                                                       │
       ┌───────────────────────────────────────────────┼───────┐
       │                                               ▼       │
       │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
       │  │  Object     │  │  Vector DB  │  │  Database   │   │
       │  │  Storage    │  │  (Index)    │  │  (Update)   │   │
       │  └─────────────┘  └─────────────┘  └─────────────┘   │
       └───────────────────────────────────────────────────────┘
```

---

## 🔐 Security Architecture

### Authentication Flow
```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Client  │────▶│  Kinde   │────▶│  Callback│────▶│  Session │
│          │     │  Auth    │     │  Handler │     │  Created │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
     │                                                   │
     │              ┌───────────────────────────────────┘
     │              │
     ▼              ▼
┌──────────────────────────────────┐
│         Protected Routes          │
│  • JWT Validation                 │
│  • Role-based Access Control      │
│  • Rate Limiting per User         │
└──────────────────────────────────┘
```

### Data Security Layers
```
┌────────────────────────────────────────────────────────────┐
│                    Security Layers                          │
├────────────────────────────────────────────────────────────┤
│  Layer 1: Transport Security (TLS 1.3)                     │
├────────────────────────────────────────────────────────────┤
│  Layer 2: Authentication (OAuth 2.0 / OIDC via Kinde)      │
├────────────────────────────────────────────────────────────┤
│  Layer 3: Authorization (RBAC + Row-Level Security)        │
├────────────────────────────────────────────────────────────┤
│  Layer 4: Data Encryption (AES-256 at rest)                │
├────────────────────────────────────────────────────────────┤
│  Layer 5: Input Validation (Zod schemas)                   │
├────────────────────────────────────────────────────────────┤
│  Layer 6: Rate Limiting (Per-user, Per-endpoint)           │
└────────────────────────────────────────────────────────────┘
```

---

## 📊 Performance Design

### Caching Strategy
```
┌─────────────────────────────────────────────────────────────┐
│                    Caching Layers                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  L1: Browser Cache (Static assets, 1 year)                  │
│      └── Images, fonts, JS bundles                          │
│                                                              │
│  L2: CDN Cache (Edge, 1 hour)                               │
│      └── Public pages, API responses                        │
│                                                              │
│  L3: Redis Cache (Application, 5-60 min)                    │
│      └── User sessions, computed roadmaps, job matches      │
│                                                              │
│  L4: Database Cache (Query, automatic)                      │
│      └── Frequent queries, connection pooling               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### Optimization Techniques
| Area | Technique | Implementation |
|------|-----------|----------------|
| **Frontend** | Code splitting | Next.js dynamic imports |
| **Frontend** | Image optimization | next/image with AVIF/WebP |
| **API** | Response compression | gzip/brotli middleware |
| **Database** | Query optimization | Indexed fields, efficient JOINs |
| **AI** | Parallel agent execution | LangGraph parallel nodes |
| **AI** | Model response caching | Hash-based cache keys |

---

## 🚀 Deployment Architecture

### Cloud Infrastructure (Google Cloud)
```
┌─────────────────────────────────────────────────────────────────┐
│                     Google Cloud Platform                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │   Cloud Run     │  │   Cloud Run     │  │   Cloud Run     │  │
│  │   (Frontend)    │  │   (Backend)     │  │   (Workers)     │  │
│  │   Next.js App   │  │   Python API    │  │   Inngest       │  │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘  │
│           │                    │                    │            │
│           └────────────────────┼────────────────────┘            │
│                                │                                 │
│  ┌─────────────────────────────▼─────────────────────────────┐  │
│  │                    Cloud Load Balancer                     │  │
│  └─────────────────────────────┬─────────────────────────────┘  │
│                                │                                 │
│  ┌──────────────┐  ┌───────────▼───────────┐  ┌──────────────┐  │
│  │ Cloud SQL    │  │    Cloud Storage      │  │ Vertex AI    │  │
│  │ (Postgres)   │  │    (Resumes/Media)    │  │ (Agents)     │  │
│  └──────────────┘  └───────────────────────┘  └──────────────┘  │
│                                                                  │
│  ┌──────────────┐  ┌───────────────────────┐  ┌──────────────┐  │
│  │ Memorystore  │  │    Secret Manager     │  │ Cloud        │  │
│  │ (Redis)      │  │    (API Keys)         │  │ Monitoring   │  │
│  └──────────────┘  └───────────────────────┘  └──────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### CI/CD Pipeline
```
┌────────────┐    ┌────────────┐    ┌────────────┐    ┌────────────┐
│   GitHub   │───▶│  GitHub    │───▶│   Build    │───▶│   Deploy   │
│   Push     │    │  Actions   │    │   & Test   │    │ Cloud Run  │
└────────────┘    └────────────┘    └────────────┘    └────────────┘
                        │
         ┌──────────────┼──────────────┐
         │              │              │
         ▼              ▼              ▼
    ┌─────────┐   ┌─────────┐   ┌─────────┐
    │  Lint   │   │  Test   │   │  Build  │
    │  Check  │   │  Suite  │   │  Image  │
    └─────────┘   └─────────┘   └─────────┘
```

---

## 📦 Module Integration Design

### Data Flow Between Modules
```
┌─────────────────────────────────────────────────────────────────┐
│                    User Profile (Central Hub)                    │
├──────────────────────────┬──────────────────────────────────────┤
│       User Data          │         Derived Data                 │
│  • Personal Info         │  • Skill Graph                       │
│  • Resume(s)             │  • Career Roadmap                    │
│  • Learning Goals        │  • Progress Metrics                  │
│  • Job Preferences       │  • Recommendations                   │
└──────────────────────────┴──────────────────────────────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌───────────────┐          ┌───────────────┐          ┌───────────────┐
│ ATS Analysis  │────────▶ │  Roadmap Gen  │────────▶ │Course Generate│
│               │ Skill    │               │ Steps    │               │
│ → Skill Gaps  │ Gaps     │ → Milestones  │          │ → Modules     │
└───────────────┘          └───────────────┘          └───────────────┘
        │                           │                           │
        │                           ▼                           │
        │                  ┌───────────────┐                    │
        │                  │Resume Optimizer│                   │
        │                  │               │                    │
        │                  │ → Updated CV  │                    │
        │                  └───────────────┘                    │
        │                           │                           │
        └───────────────────────────┼───────────────────────────┘
                                    │
                                    ▼
                          ┌───────────────┐
                          │Mock Interview │
                          │               │
                          │ → Feedback    │
                          │ → Rerouting   │
                          └───────────────┘
```

---

## 🧪 Testing Strategy

### Test Pyramid
```
                    ┌─────────────┐
                    │   E2E Tests │  5%
                    │  (Playwright)│
                    └──────┬──────┘
                           │
                ┌──────────▼──────────┐
                │  Integration Tests  │  25%
                │      (Vitest)       │
                └──────────┬──────────┘
                           │
          ┌────────────────▼────────────────┐
          │          Unit Tests              │  70%
          │    (Vitest + React Testing Lib)  │
          └──────────────────────────────────┘
```

### Testing Scope
| Component | Test Type | Tools |
|-----------|-----------|-------|
| React Components | Unit | Vitest, RTL |
| tRPC Procedures | Integration | Vitest |
| Python Agents | Unit | pytest |
| LangGraph Flows | Integration | pytest |
| API Endpoints | E2E | Playwright |
| User Flows | E2E | Playwright |

---

## 📈 Observability

### Monitoring Stack
```
┌─────────────────────────────────────────────────────────────┐
│                    Observability Platform                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Metrics (Cloud Monitoring)                                  │
│  └── Request latency, error rates, throughput               │
│  └── AI model latency, token usage, costs                   │
│  └── Background job success/failure rates                   │
│                                                              │
│  Logging (Cloud Logging)                                     │
│  └── Structured JSON logs                                    │
│  └── Request tracing IDs                                     │
│  └── Error stack traces                                      │
│                                                              │
│  Tracing (Cloud Trace)                                       │
│  └── End-to-end request traces                              │
│  └── Cross-service correlation                              │
│                                                              │
│  Alerting                                                    │
│  └── PagerDuty integration                                  │
│  └── Slack notifications                                     │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 Technical Decisions Log

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Frontend Framework | Next.js 14+ | App Router, RSC, built-in optimization |
| API Layer | tRPC + Hono | Type safety, performance |
| Database ORM | Drizzle | Type-safe, performant, edge-ready |
| Auth Provider | Kinde | Simple OAuth, free tier |
| AI Orchestration | LangGraph | Multi-agent, parallel execution |
| Background Jobs | Inngest | Durable, event-driven, easy |
| Vector DB | Supabase pgvector | Integrated with existing stack |
| Deployment | Cloud Run | Serverless, auto-scaling, cost-effective |
| Video Interviews | Tavus | AI avatar, realistic experience |

---

## 🔄 Future Considerations

### Planned Enhancements
1. **Mobile App**: React Native with shared business logic
2. **Offline Mode**: Service workers for offline learning
3. **Multi-language**: i18n for global expansion
4. **B2B Dashboard**: Institutional admin features
5. **Advanced Analytics**: Learning path optimization ML

### Technical Debt Items
- [ ] Migrate remaining pages to App Router
- [ ] Consolidate state management patterns
- [ ] Improve test coverage (target: 80%)
- [ ] Optimize LLM token usage
- [ ] Implement request batching

---

*Document maintained by Edify-AI Engineering Team*
